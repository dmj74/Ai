name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    name: Build & publish release APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Accept licenses & install SDK packages
        run: |
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Prepare wrapper
        run: chmod +x gradlew

      - name: Unit tests + APKs
        run: ./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease --no-daemon --stacktrace

      - name: Name the installable APK
        run: cp app/build/outputs/apk/debug/app-debug.apk "titanali-${GITHUB_REF_NAME}.apk"

      - name: Publish GitHub release with APK
        uses: softprops/action-gh-release@v2
        with:
          # The debug-signed APK installs directly on phones; the unsigned
          # release APK is attached for anyone who wants to sign it themselves.
          files: |
            titanali-*.apk
            app/build/outputs/apk/release/app-release-unsigned.apk
          generate_release_notes: true
