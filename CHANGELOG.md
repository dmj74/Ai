# تغییرات / Changelog

## [1.2.0] — 2026-09-23

### اضافه شد
- دو بک‌اند **بدون کلید**: Pollinations (پیش‌فرض و آمادهٔ استفاده) و LLM7؛ کلید/توکن شخصی برای سقف بالاتر اختیاری است.
- فهرست مدل Pollinations و مدل‌های رایگان LLM7 از endpoint رسمی هر سرویس خوانده می‌شود.
- تشخیص پاسخ کامل غیر استریم برای gatewayهایی که `stream=true` را نادیده می‌گیرند.
- نمایش روشن «کلید اختیاری» و توضیح سرویس‌های بدون کلید در تنظیمات.
- failover خودکار از Pollinations به LLM7 پیش از شروع پاسخ، وقتی سهمیه یا endpoint سرویس اول موقتاً در دسترس نیست.
- نسخهٔ برنامه به 1.2.0 / versionCode 3 رسید.

### امنیت
- هیچ API key خصوصی داخل APK یا مخزن قرار داده نشده است؛ کلیدهای سرویس‌ها شخصی‌اند و باید از لینک رسمی ساخته شوند.

## [1.1.0] — 2026-09-23

### اضافه شد
- دو سرویس رایگان جدید: **Cerebras** و **Mistral** (جمعاً ۷ بک‌اند).
- دکمهٔ **آزمایش اتصال** در تنظیمات + نمایش وضعیت با پیام بومی‌شده.
- **بروزرسانی زندهٔ فهرست مدل‌ها** از خود سرویس‌ها (`/models`) با دکمهٔ رفرش.
- کلید API و مدل **به‌تفکیک هر سرویس** ذخیره می‌شود (سوییچ بدون گم‌شدن تنظیمات).
- پیام‌های خطای بومی‌شده (کلید نامعتبر، مدل نیافت، سقف نرخ، قطعی نت، …).
- Snackbar برای خطاهای صوتی (اجازهٔ میکروفون، چیزی شنیده نشد، نبود شنودگر).
- کپی متن پیام‌ها با انتخاب (SelectionContainer).
- تست‌های واحد JVM برای پارسر SSE، نگاشت خطاها، درس‌ها، پرامپت‌ها و registry.
- ورک‌فلوهای تمیز `ci.yml` (تست + APK دیباگ + APK ریلیز مینیفای‌شده + lint) و `release.yml` (انتشار با تگ).
- آیکون monochrome (تم اندروید ۱۳+)، `ic_launcher_round`، قوانین backup برای مستثناکردن کلیدها.
- CHANGELOG.md و بازنگری کامل README.

### تغییر کرد
- مدل‌های پیش‌فرض به‌روز شد: Groq → `openai/gpt-oss-120b` (مدل‌های Llama در Groq دیگر رایگان نیستند)، Gemini → خانوادهٔ `gemini-2.5`، OpenRouter → `openrouter/free` و مدل‌های `:free` فعلی.
- endpoint های Hugging Face به `router.huggingface.co` مهاجرت کرد (آدرس قدیمی دیگر وجود ندارد).
- APK ریلیز با R8 مینیفای و shrink می‌شود؛ آواتار از ۲MB به ~۰٫۵MB کاهش یافت.
- Room به نسخهٔ ۲ با ایندکس روی `messages.convId` (+ migration).
- edge-to-edge در MainActivity.

### درست شد
- سقوط هنگام ساخت `SpeechRecognizer` (رفلکشن اشتباه) → استفاده از API رسمی `createSpeechRecognizer`.
- کالبک‌های TTS حالا روی رشتهٔ اصلی اجرا می‌شوند (جلوگیری از crash حالت Compose).
- توقف استریم دیگر به‌اشتباه بنر خطا نشان نمی‌دهد و متن ناقص ذخیره می‌شود.
- حلقهٔ مکالمهٔ صوتی با محدودیت تلاش مجدد در سکوت و بازگشت سالم به حالت Idle.

### حذف شد
- ورک‌فلوهای تکراری و اسکریپت‌های push لاگ به شاخهٔ `build-logs` / gist / paste (ریسک افشای لاگ).

## [1.0.0] — 2026-09-22
- اولین نسخه: چت استریمینگ، تیتانالی (متنی/صوتی)، آموزش زبان سه‌زبانه، پنج بک‌اند رایگان، Room، دوزبانه fa/en.
